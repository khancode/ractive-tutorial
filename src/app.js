import Ractive from 'ractive';

// execute this code with the play button in the top-right corner of this pane
const ractive = new Ractive({
  target: '#target',
  template: '#template'
});

// throughout these tutorials, there is an output div with an id of 'target'
// and a template script with an id of 'template'
// you can find the output div and template script on the HTML tab
